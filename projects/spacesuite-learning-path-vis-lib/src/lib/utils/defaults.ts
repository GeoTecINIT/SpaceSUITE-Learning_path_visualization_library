import { LearningPathStyleConfig } from '../models';

export const DEFAULT_STYLE_CONFIG: LearningPathStyleConfig = {
  textColor: '#222',
  backgroundColor: '#ffffff',
  completedColor: '#4CAF50',
  inProgressColor: '#FFC107',
  pendingColor: '#BDBDBD',
  cardHeight: 200,
  cardWidth: 450,
  gap: 20,
};
